import { MenuItem } from '@mui/material';
import styled from 'styled-components';

export const Wrapper = styled.div`
  .MuiInputBase-root {
    font-weight: bold;
    color: #ffe37e;
  }
`;

export const StyledCurrencyMenuItem = styled(MenuItem)`
  font-weight: bold !important;
  color: #ffe37e !important;
`;
